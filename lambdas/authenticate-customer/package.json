{
	"name": "authenticate-customer",
	"version": "1.0.0",
	"description": "Lambda function to authenticate customer",
	"main": "index.js",
	"scripts": {
		"build": "npm install && zip -r authenticate-customer.zip ."
	},
	"dependencies": {
		"pg": "^8.13.1"
	}
}
